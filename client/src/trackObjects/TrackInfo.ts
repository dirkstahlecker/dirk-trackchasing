export class TrackInfo
{
	//Create a typesafe representation of the information from the server, which is then used elsewhere
	//Ideally no other component will need to make track info server calls since it's all handled here
	public static async fetchInfo(): Promise<void> //TODO on the type
	{

	}
}